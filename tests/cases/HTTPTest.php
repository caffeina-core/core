<?php

use Core\{
  HTTP
};

class HTTPTest extends PHPUnit_Framework_TestCase {

    public function testRequest() {
      /*
      $results = HTTP::get("https://www.apple.com");
      var_dump(
        $results
      );

      die;
      */

      //$this->assertTrue(Hash::verify($payload, $hash, $testAlgo));
    }


}
